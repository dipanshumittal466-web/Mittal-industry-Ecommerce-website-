# पहले .gitignore फाइल बनाओ
echo "# Node.js dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build output
dist/
build/
.next/
out/

# IDE settings
.vscode/
.idea/

# OS generated files
.DS_Store
Thumbs.db" > .gitignore


# Git को बताओ कि .env अब से repo में नहीं जाना चाहिए
git rm --cached .env

# .gitignore को stage करो
git add .gitignore

# Commit करो
git commit -m "Added .gitignore to ignore env and build files"

# Push करो
git push origin main
